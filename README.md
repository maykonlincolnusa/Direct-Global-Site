
  # maykonlincoln

  This is a code bundle for maykonlincoln. The original project is available at https://www.figma.com/design/wf01gCjzvmdSScQuopWxB2/maykonlincoln.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  